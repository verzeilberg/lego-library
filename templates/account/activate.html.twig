{% extends '/base.html.twig' %}
{% block content %}
    <div class="container-fluid">
        <div class="row">
            <div class="col text-center border-bottom mb-2">
                <h1>{{ title }}</h1>
            </div>
        </div>
        <div class="row">
            <div class="col text-center">
                <p>{{ message }}</p>
            </div>
        </div>
    </div>
{% endblock %}
